import React from 'react';
import RankingItem from './RankingItem';

const RankingItems = ({ leftData }) => {
  return (
    <>
      {/* {leftData.map((d) => (
        <RankingItem img={d.img} id={d.id} brand={d.brand} />
      ))} */}
    </>
  );
};

export default RankingItems;
